<h1>Listing event_items</h1>

<table>
  <thead>
    <tr>
      <th>Event</th>
      <th>Supply item</th>
      <th>Count</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @event_items.each do |event_item| %>
      <tr>
        <td><%= event_item.event %></td>
        <td><%= event_item.supply_item %></td>
        <td><%= event_item.count %></td>
        <td><%= link_to 'Show', event_item %></td>
        <td><%= link_to 'Edit', edit_event_item_path(event_item) %></td>
        <td><%= link_to 'Destroy', event_item, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Event item list', new_event_item_path %>
